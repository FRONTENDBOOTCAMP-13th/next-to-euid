{
  "name": "vite-css",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "all": "run-s clean build preview",
    "start": "live-server ./src --port=8090 --host=localhost --no-browser"
  },
  "devDependencies": {
    "braces": "^3.0.3",
    "live-server": "^1.2.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.5.3",
    "vite": "^6.2.2",
    "watch": "^0.13.0"
  }
}
